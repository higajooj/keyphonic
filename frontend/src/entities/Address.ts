export type Address = {
  id: string;
  street: string;
  number: string;
  neighborhood: string;
  zip_code: string;
  complement: string | null;
  city: string;
  state: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
};
